<UserControl x:Class="C_TestForge.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:C_TestForge.UI.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="{StaticResource AppBackground}">
    
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Welcome Header -->
            <materialDesign:Card Grid.Row="0" Margin="0,0,0,24" UniformCornerRadius="8"
                               Background="{StaticResource AccentColor}">
                <Grid>
                    <Image Source="/Resources/Images/dashboard-header-bg.jpg" Stretch="Fill" Opacity="0.2"/>
                    <StackPanel Margin="24">
                        <TextBlock Text="Welcome to C-TestForge" 
                                   FontSize="32" FontWeight="Light" Foreground="White"/>
                        <TextBlock Text="C Source Code Analysis and Test Case Generation Tool" 
                                   FontSize="18" Foreground="White" Opacity="0.9" Margin="0,8,0,0"/>
                    </StackPanel>
                </Grid>
            </materialDesign:Card>
            
            <!-- Project Info Cards (multiple projects) -->
            <ItemsControl Grid.Row="1" Margin="0,0,0,24"
                          ItemsSource="{Binding AvailableProjects}"
                          Visibility="{Binding AvailableProjects.Count, Converter={StaticResource CountToVisibilityConverter}}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <materialDesign:Card Margin="0,0,0,16" UniformCornerRadius="8" materialDesign:ShadowAssist.ShadowDepth="Depth1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <!-- Project Icon -->
                                <Border Grid.Column="0" Background="{StaticResource AccentLight}" Width="100">
                                    <materialDesign:PackIcon Kind="FolderOpenOutline" Width="64" Height="64" 
                                                           Foreground="White" HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"/>
                                </Border>
                                <!-- Project Details -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="24,16">
                                    <TextBlock Text="Project" FontSize="14" Foreground="{StaticResource SecondaryText}"/>
                                    <TextBlock Text="{Binding Name}" FontSize="24" FontWeight="Medium" Margin="0,4,0,8"/>
                                    <WrapPanel Orientation="Horizontal">
                                        <StackPanel Orientation="Horizontal" Margin="0,0,24,0">
                                            <materialDesign:PackIcon Kind="FileOutline" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                            <TextBlock Text="{Binding SourceFiles.Count, StringFormat='{}{0} source files'}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <!-- Add more project stats here if needed -->
                                    </WrapPanel>
                                </StackPanel>
                                <!-- Quick Actions -->
                                <Border Grid.Column="2" Background="#F5F5F5" Width="200">
                                    <StackPanel VerticalAlignment="Center" Margin="16">
                                        <Button Style="{StaticResource PrimaryButtonStyle}" Content="Xem dự án"
                                               Command="{Binding DataContext.ViewProjectCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                               CommandParameter="{Binding}" Margin="0,0,0,12"
                                               HorizontalAlignment="Stretch"/>
                                        <Button Style="{StaticResource SecondaryButtonStyle}" Content="Đóng dự án"
                                               Command="{Binding DataContext.CloseProjectCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                               CommandParameter="{Binding}" HorizontalAlignment="Stretch"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </materialDesign:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            
            <!-- No Project Card (when no project is open) -->
            <materialDesign:Card Grid.Row="1" Margin="0,0,0,24" UniformCornerRadius="8"
                               Visibility="{Binding AvailableProjects.Count, Converter={StaticResource InverseCountToVisibilityConverter}}"
                               materialDesign:ShadowAssist.ShadowDepth="Depth1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Empty State Message -->
                    <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="24,32,24,24">
                        <materialDesign:PackIcon Kind="FolderOpenOutline" Width="80" Height="80" 
                                               HorizontalAlignment="Center" Foreground="{StaticResource DisabledText}"/>
                        <TextBlock Text="No Project Open" Style="{StaticResource HeadingStyle}"
                                 HorizontalAlignment="Center" Margin="0,16,0,8"/>
                        <TextBlock Text="Start by creating a new project or open an existing one"
                                 Style="{StaticResource BodyTextStyle}" Foreground="{StaticResource SecondaryText}"
                                 HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" 
                                 MaxWidth="400"/>
                    </StackPanel>
                    
                    <!-- Action Buttons -->
                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,32">
                        <Button Content="Create New Project" Command="{Binding NewProjectCommand}"
                               Style="{StaticResource PrimaryButtonStyle}" Margin="8,0">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="FilePlusOutline" Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>
                        <Button Content="Open Project" Command="{Binding OpenProjectCommand}"
                               Style="{StaticResource SecondaryButtonStyle}" Margin="8,0">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="FolderOpenOutline" Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>
                    </StackPanel>
                </Grid>
            </materialDesign:Card>
            
            <!-- Feature Cards -->
            <ItemsControl Grid.Row="2" Margin="0,0,0,24">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                
                <!-- Source Analysis Card -->
                <materialDesign:Card Style="{StaticResource ModernCardStyle}" Width="380" Height="220" Margin="0,8,16,8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Card Header -->
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                            <Border Width="40" Height="40" Background="{StaticResource AccentLight}" 
                                   CornerRadius="20" Margin="0,0,16,0">
                                <materialDesign:PackIcon Kind="CodeBraces" Width="24" Height="24" 
                                                       Foreground="White" HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Source Code Analysis" FontSize="18" FontWeight="Medium" 
                                     VerticalAlignment="Center"/>
                        </StackPanel>
                        
                        <!-- Card Content -->
                        <TextBlock Grid.Row="1" Style="{StaticResource BodyTextStyle}" TextWrapping="Wrap">
                            Parse and analyze C source code structure. Detect variables, functions,
                            definitions, and preprocessing conditions to support test case generation.
                        </TextBlock>
                        
                        <!-- Card Action -->
                        <Button Grid.Row="2" Content="View Analysis" HorizontalAlignment="Right" 
                               Style="{StaticResource SecondaryButtonStyle}"
                               Command="{Binding NavigateCommand}" CommandParameter="SourceAnalysis"/>
                    </Grid>
                </materialDesign:Card>
                
                <!-- Test Case Generation Card -->
                <materialDesign:Card Style="{StaticResource ModernCardStyle}" Width="380" Height="220" Margin="0,8,16,8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Card Header -->
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                            <Border Width="40" Height="40" Background="{StaticResource AccentLight}" 
                                   CornerRadius="20" Margin="0,0,16,0">
                                <materialDesign:PackIcon Kind="TestTube" Width="24" Height="24" 
                                                       Foreground="White" HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Test Case Generation" FontSize="18" FontWeight="Medium" 
                                     VerticalAlignment="Center"/>
                        </StackPanel>
                        
                        <!-- Card Content -->
                        <TextBlock Grid.Row="1" Style="{StaticResource BodyTextStyle}" TextWrapping="Wrap">
                            Automatically generate test cases for functions based on source code analysis. 
                            Uses Z3 Solver to find optimal input values for code testing.
                        </TextBlock>
                        
                        <!-- Card Action -->
                        <Button Grid.Row="2" Content="Manage Test Cases" HorizontalAlignment="Right" 
                               Style="{StaticResource SecondaryButtonStyle}"
                               Command="{Binding NavigateCommand}" CommandParameter="TestCases"/>
                    </Grid>
                </materialDesign:Card>
                
                <!-- Type Mapping Card -->
                <materialDesign:Card Style="{StaticResource ModernCardStyle}" Width="380" Height="220" Margin="0,8,16,8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Card Header -->
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                            <Border Width="40" Height="40" Background="{StaticResource AccentLight}" 
                                   CornerRadius="20" Margin="0,0,16,0">
                                <materialDesign:PackIcon Kind="SwapHorizontal" Width="24" Height="24" 
                                                       Foreground="White" HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Type Mapping Management" FontSize="18" FontWeight="Medium" 
                                     VerticalAlignment="Center"/>
                        </StackPanel>
                        
                        <!-- Card Content -->
                        <TextBlock Grid.Row="1" Style="{StaticResource BodyTextStyle}" TextWrapping="Wrap">
                            Manage mappings from user-defined types to basic types and define value constraints
                            for each data type to support accurate test case generation.
                        </TextBlock>
                        
                        <!-- Card Action -->
                        <Button Grid.Row="2" Content="Configure Types" HorizontalAlignment="Right" 
                               Style="{StaticResource SecondaryButtonStyle}"
                               Command="{Binding NavigateCommand}" CommandParameter="TypeMappings"/>
                    </Grid>
                </materialDesign:Card>
                
                <!-- Function Analysis Card -->
                <materialDesign:Card Style="{StaticResource ModernCardStyle}" Width="380" Height="220" Margin="0,8,16,8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Card Header -->
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                            <Border Width="40" Height="40" Background="{StaticResource AccentLight}" 
                                   CornerRadius="20" Margin="0,0,16,0">
                                <materialDesign:PackIcon Kind="Function" Width="24" Height="24" 
                                                       Foreground="White" HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Function Analysis" FontSize="18" FontWeight="Medium" 
                                     VerticalAlignment="Center"/>
                        </StackPanel>
                        
                        <!-- Card Content -->
                        <TextBlock Grid.Row="1" Style="{StaticResource BodyTextStyle}" TextWrapping="Wrap">
                            Detailed analysis of functions in source code, including parameters, return values,
                            complexity, and dependencies between functions.
                        </TextBlock>
                        
                        <!-- Card Action -->
                        <Button Grid.Row="2" Content="View Functions" HorizontalAlignment="Right" 
                               Style="{StaticResource SecondaryButtonStyle}"
                               Command="{Binding NavigateCommand}" CommandParameter="Functions"/>
                    </Grid>
                </materialDesign:Card>
            </ItemsControl>
            
            <!-- Footer Info -->
            <Border Grid.Row="3" BorderBrush="{StaticResource BorderLight}" 
                   BorderThickness="0,1,0,0" Padding="0,20,0,0" Margin="0,8,0,0">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="C-TestForge v1.0" FontSize="14" Foreground="{StaticResource SecondaryText}"
                             HorizontalAlignment="Center"/>
                    <TextBlock Text="Developed with .NET 9 and ClangSharp" FontSize="12" 
                             Foreground="{StaticResource DisabledText}" HorizontalAlignment="Center" 
                             Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>