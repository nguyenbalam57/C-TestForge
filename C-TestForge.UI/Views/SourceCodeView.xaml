<UserControl x:Class="C_TestForge.UI.Views.SourceCodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:C_TestForge.UI.Views"
             xmlns:viewModels="clr-namespace:C_TestForge.UI.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewModels:SourceCodeViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewModels:SourceCodeViewModel />
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Content="Open File" Command="{Binding LoadFileCommand}" Margin="8" Width="120" Height="32"/>
        <ScrollViewer Grid.Row="1" Margin="8">
            <TextBox Text="{Binding SourceCode, UpdateSourceTrigger=PropertyChanged}" FontFamily="Consolas" FontSize="14" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" IsReadOnly="True"/>
        </ScrollViewer>
    </Grid>
</UserControl>