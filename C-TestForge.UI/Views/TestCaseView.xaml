<UserControl x:Class="C_TestForge.UI.Views.TestCaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:C_TestForge.UI.Views"
             xmlns:viewModels="clr-namespace:C_TestForge.UI.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewModels:TestCaseViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewModels:TestCaseViewModel />
    </UserControl.DataContext>
    <Grid Margin="24">
        <StackPanel>
            <TextBlock Text="Test Case Detail" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>
            <TextBlock Text="Name:" FontWeight="SemiBold"/>
            <TextBlock Text="{Binding TestCase.Name}" Margin="0,0,0,8"/>
            <TextBlock Text="Description:" FontWeight="SemiBold"/>
            <TextBlock Text="{Binding TestCase.Description}" Margin="0,0,0,8"/>
            <TextBlock Text="Function:" FontWeight="SemiBold"/>
            <TextBlock Text="{Binding TestCase.FunctionName}" Margin="0,0,0,8"/>
            <TextBlock Text="Type:" FontWeight="SemiBold"/>
            <TextBlock Text="{Binding TestCase.Type}" Margin="0,0,0,8"/>
            <TextBlock Text="Status:" FontWeight="SemiBold"/>
            <TextBlock Text="{Binding TestCase.Status}" Margin="0,0,0,8"/>
        </StackPanel>
    </Grid>
</UserControl>